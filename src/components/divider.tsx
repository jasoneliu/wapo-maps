import React from 'react';

const Divider = () => {
  return (
    <div
      style={{
        height: '1px',
        width: '100%',
        backgroundColor: 'var(--color-ui-gray-light)',
      }}
    />
  );
};

export default Divider;
